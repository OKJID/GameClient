--- Platform/MacOS/Source/Metal/MetalDevice8.mm
+++ Platform/MacOS/Source/Metal/MetalDevice8.mm
@@ -940,8 +940,11 @@
   lu.lightingEnabled = m_RenderStates[D3DRS_LIGHTING];
-  lu.diffuseSource = m_RenderStates[D3DRS_DIFFUSEMATERIALSOURCE];
-  lu.ambientSource = m_RenderStates[D3DRS_AMBIENTMATERIALSOURCE];
-  lu.specularSource = m_RenderStates[D3DRS_SPECULARMATERIALSOURCE];
-  lu.emissiveSource = m_RenderStates[D3DRS_EMISSIVEMATERIALSOURCE];
+  bool hasVertColor1 = (fvf & D3DFVF_DIFFUSE) != 0;
+  bool hasVertColor2 = (fvf & 0x080) != 0; // D3DFVF_SPECULAR
+  lu.diffuseSource = hasVertColor1 ? m_RenderStates[D3DRS_DIFFUSEMATERIALSOURCE] : D3DMCS_MATERIAL;
+  lu.ambientSource = hasVertColor1 ? m_RenderStates[D3DRS_AMBIENTMATERIALSOURCE] : D3DMCS_MATERIAL;
+  lu.specularSource = hasVertColor2 ? m_RenderStates[D3DRS_SPECULARMATERIALSOURCE] : D3DMCS_MATERIAL;
+  lu.emissiveSource = hasVertColor1 ? m_RenderStates[D3DRS_EMISSIVEMATERIALSOURCE] : D3DMCS_MATERIAL;
   lu.hasNormals = (fvf & D3DFVF_NORMAL) ? 1 : 0;
